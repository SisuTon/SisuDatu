import pytest
from sisu_bot.bot.services import yandex_speechkit_tts

def test_generate_speech():
    # Этот тест будет требовать настоящие API ключи или мокирование HTTP запросов.
    # Для начала просто убедимся, что функция существует.
    try:
        # В реальном тесте здесь нужно мокать вызовы внешних API
        pass
    except Exception as e:
        pytest.fail(f"generate_speech вызвал исключение: {e}")

# Добавьте другие тесты для функций yandex_speechkit_tts здесь 